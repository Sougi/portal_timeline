<?php

namespace Tests\Feature;

use App\Post;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class PostTest extends TestCase
{
    use RefreshDatabase;
    /**
     * A basic feature test example.
     *
     * @return void
     */
    public function testSavePost()
    {
        // $post = new Post() ; 
        // $post->title = "new te" ; 
        // $post->content='bal bal bla bal  alnba' ; 
        // $post->slug = 'efgfgteg'  ;
        // $post->active= 'false' ; 
        // $post->body = 'ba lb lab alba lha ' ;

        // $post->save();

        // $this->assertDatabaseHas('posts' , [
        //     'title' => 'new te'
        // ]) ; 
    }
}
